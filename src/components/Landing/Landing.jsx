import React from 'react';
import PropTypes from 'prop-types';

import PleaseLogin from '../Profile/PleaseLogin';

const Landing = props => {
  return <PleaseLogin />;
};

Landing.propTypes = {};

export default Landing;
